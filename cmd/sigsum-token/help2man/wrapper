#!/bin/bash

set -eu
cd "$(dirname "$0")"

opt=$1; shift
case "$opt" in
	--help)
		../../../build/sigsum-token --help
		;;
	--version)
		../../../build/sigsum-token --version
		;;
	*)
		echo "invalid option: $opt"
		exit 1
		;;
esac
