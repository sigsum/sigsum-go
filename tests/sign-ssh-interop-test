#! /bin/sh

set -e

./sigsum-key gen -o test.key

echo foo > test.msg
rm -f test.msg.sig

./sigsum-key sign --ssh -k test.key -n sigsum-test -o test.msg.sig < test.msg
ssh-keygen -q -Y check-novalidate -f test.key.pub -n sigsum-test -s test.msg.sig < test.msg
